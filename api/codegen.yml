overwrite: true
schema: "./src/graphql/schemas"
documents: null
generates:
    src/graphql/types.d.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
    ./graphql.schema.json:
        plugins:
            - "introspection"
config:
    onlyOperationTypes: true
    avoidOptionals: true
