type AuthResponse {
	success: Boolean!
	message: String!
	token: String
	role: ROLE
}

input LoginInput {
	email: String
	password: String
}

type MeInfoResponse {
	user: User!
	company: Company!
}

extend type Mutation {
	meInfo: MeInfoResponse
}
