type LoginResponse {
	success: Boolean
	message: String
	token: String
	role: ROLE
}

input LoginInput {
	email: String
	password: String
}

extend type Mutation {
	login(input: LoginInput!): LoginResponse!
}
