type AuthResponse {
	success: Boolean!
	message: String!
	token: String
	role: ROLE
}

input LoginInput {
	email: String
	password: String
}

type LoggedInUserResponse {
	user: User!
	company: Company!
}

extend type Query {
	loggedInUser: LoggedInUserResponse
}
