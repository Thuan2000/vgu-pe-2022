overwrite: true
schema: "http://localhost:8080/graphql"
documents: null
generates:
  src/graphql/types.graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
config:
  enumsAsTypes: true
  skipTypename: true
  typesPrefix: I
