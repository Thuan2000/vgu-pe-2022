query Services($input: FetchServicesInput) {
  services(input: $input) {
    services {
      id
      coverImage
      location
      name
      price
      minPrice
      maxPrice
      rating
    }
    count
  }
}
