mutation UploadFiles($input: UploadFilesInput!) {
  uploadFiles(input: $input) {
    url
    location
    fileName
    fileType
  }
}

mutation DeleteFile($location: String!) {
  deleteFile(location: $location) {
    message
    success
  }
}
